server:
  port: 8081

spring:
  application:
    name: natural-service
  datasource:
    url: jdbc:postgresql://localhost:5432/naturalmedic
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
